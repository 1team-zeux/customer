server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location ~ ^/(orders|chaos) {
        proxy_pass http://order-service:17001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
